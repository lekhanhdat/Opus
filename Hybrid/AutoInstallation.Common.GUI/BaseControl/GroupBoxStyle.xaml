<!--/********************************************************************
 *
 *  PROPRIETARY and CONFIDENTIAL
 *
 *  This file is licensed from, and is a trade secret of:
 *
 *                   AvePoint, Inc.
 *                   525 Washington Blvd, Suite 1400
 *                   Jersey City, NJ 07310
 *                   United States of America
 *                   Telephone: +1-201-793-1111
 *                   WWW: www.avepoint.com
 *
 *  Refer to your License Agreement for restrictions on use,
 *  duplication, or disclosure.
 *
 *  RESTRICTED RIGHTS LEGEND
 *
 *  Use, duplication, or disclosure by the Government is
 *  subject to restrictions as set forth in subdivision
 *  (c)(1)(ii) of the Rights in Technical Data and Computer
 *  Software clause at DFARS 252.227-7013 (Oct. 1988) and
 *  FAR 52.227-19 (C) (June 1987).
 *
 *  Copyright © 2017-2026 AvePoint® Inc. All Rights Reserved. 
 *
 *  Unpublished - All rights reserved under the copyright laws of the United States.
 */-->


<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="BCStyleLink.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- SimpleStyles: GroupBox -->

    <SolidColorBrush x:Key="TitleForeground" Color="#FF224455" />
    <SolidColorBrush x:Key="GroupBorder" Color="#FF999999" />
    <!--<Style x:Key="{x:Type GroupBox}" TargetType="{x:Type GroupBox}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type GroupBox}">
        
			
			<Grid>
			<StackPanel x:Name="TitlePanel" VerticalAlignment="Top" Canvas.ZIndex="1">

				<TextBlock x:Name="TitleTextBlock" Foreground="#FF224455" Text="Group Box" FontSize="11" FontWeight="SemiBold" Margin="10,0,0,0" HorizontalAlignment="Left" Padding="3,2" Background="White" VerticalAlignment="Center" />
			</StackPanel>
            <Border BorderBrush="#FF999999" BorderThickness="1" CornerRadius="1" VerticalAlignment="Stretch" Margin="0,10,0,0" >
			<ContentPresenter Margin="5,8,10,5" />
            </Border>
            </Grid>
			
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>-->


    <BorderGapMaskConverter x:Key="BorderGapMaskConverter" />
    <Style x:Key="{x:Type GroupBox}"
           TargetType="{x:Type GroupBox}">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Foreground="#FF333333" FontWeight="Bold" FontSize="12"
                               FontFamily="Arial" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush"
                Value="#FF888888" />
        <Setter Property="Foreground"
                Value="#FF333333" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupBox}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="4" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="8" />
                        </Grid.RowDefinitions>
                        <Border CornerRadius="4"
                                Grid.Row="1"
                                Grid.RowSpan="3"
                                Grid.Column="0"
                                Grid.ColumnSpan="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="Transparent"
                                Background="{TemplateBinding Background}" />
                        <!-- ContentPresenter for the header -->
                        <Border x:Name="Header"
                                Padding="3,0,3,0"
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="1">
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <!-- Primary content for GroupBox -->
                        <ContentPresenter Grid.Row="2"
                                          Grid.Column="1"
                                          Grid.ColumnSpan="2"
                                          Margin="{TemplateBinding Padding}" />
                        <Border SnapsToDevicePixels="True" CornerRadius="1"
                                Grid.Row="1"
                                Grid.RowSpan="3"
                                Grid.ColumnSpan="4"
                                BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="#FFCCCCCC">
                            <Border.OpacityMask>
                                <MultiBinding Converter="{StaticResource BorderGapMaskConverter}"
                                              ConverterParameter="8">
                                    <Binding ElementName="Header"
                                             Path="ActualWidth" />
                                    <Binding RelativeSource="{RelativeSource Self}"
                                             Path="ActualWidth" />
                                    <Binding RelativeSource="{RelativeSource Self}"
                                             Path="ActualHeight" />
                                </MultiBinding>
                            </Border.OpacityMask>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="2,0,0,0" />
    </Style>

    <!--<Style x:Key="{x:Type GroupBox}" TargetType="{x:Type GroupBox}">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Foreground="#FF224455" FontWeight="Bold" FontFamily="Arail"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupBox}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="24"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border BorderThickness="1,1,1,0" BorderBrush="{DynamicResource NormalBorderBrush}" Grid.Row="0" CornerRadius="1,1,0,0" Margin="0,12,0,0"/>
                        <Border BorderThickness="1,0,1,1" BorderBrush="{DynamicResource NormalBorderBrush}" Grid.Row="1" CornerRadius="0,0,1,1"/>
                        <Grid HorizontalAlignment="Left" Margin="10,0" Width="Auto" Background="{DynamicResource WhiteBrush}" VerticalAlignment="Center">
                            <ContentPresenter 
						Grid.Row="0"				
						RecognizesAccessKey="True" HorizontalAlignment="Stretch" d:LayoutOverrides="Width" Margin="3,0" ContentSource="Header">

                            </ContentPresenter>
                        </Grid>
                        <ContentPresenter 
				    Grid.Row="1"
	                Margin="6,-5,6,7" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->


</ResourceDictionary>